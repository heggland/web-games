{
  "name": "hegglands-games",
  "version": "1.0.0",
  "description": "",
  "type": "commonjs",
  "scripts": {
    "tsc": "tsc",
    "dev": "concurrently \"npm run watch\" \"npm run start\"",
    "watch": "esbuild src/**/* --outdir=dist --bundle --sourcemap --platform=node --external:esbuild --format=cjs --watch",
    "start": "node dist/server.js",
    "build": "esbuild src/**/* --outdir=dist --bundle --minify --sourcemap --platform=node --external:esbuild --format=cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.1",
    "esbuild": "^0.24.2",
    "nodemon": "^3.1.9",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
